name: Stage 2 Backfill

on:
  workflow_dispatch:
    inputs:
      days:
        description: 'Days to backfill (default 7)'
        required: false
        default: '7'
      from_date:
        description: 'From date YYYY-MM-DD (overrides days)'
        required: false
      to_date:
        description: 'To date YYYY-MM-DD'
        required: false

jobs:
  backfill:
    name: Seed Historical Stage 2 Data
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.10', cache: pip, cache-dependency-path: scripts/requirements.txt }
      - run: pip install -r scripts/requirements.txt

      - name: Run backfill (date range)
        if: ${{ github.event.inputs.from_date != '' }}
        run: python scripts/backfill_stage2.py --from ${{ github.event.inputs.from_date }} --to ${{ github.event.inputs.to_date || '' }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Run backfill (days)
        if: ${{ github.event.inputs.from_date == '' }}
        run: python scripts/backfill_stage2.py --days ${{ github.event.inputs.days || '7' }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
